# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm uninstall react --legacy-peer-deps && npm uninstall react react-dom ---legacy-peer-deps && npm uninstall react-bootstrap --legacy-peer-deps && npm uninstall firebase --legacy-peer-deps &&  npm uninstall react-firebase --legacy-peer-deps
      - run: npm uninstall react-google-login --legacy-peer-deps
      - run: >-
          npm install --legacy-peer-deps && npm install react@16.0.0.0 --legacy-peer-deps && npm install react react-dom  && npm install react-bootstrap --legacy-peer-deps && npm
          install firebase --legacy-peer-deps && npm install react-webcam --legacy-peer-deps && npm install react-google-login --legacy-peer-deps && npm ci  &&  npm run build --legacy-peer-deps
      - run:  npm audit fix --force 
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_TEACHERCOMDB }}'
          channelId: live
          projectId: teachercomdb
